require_relative '../lib/dark_trader'

describe "the login_twitter method" do
  it "should return client, and client is not nil" do
    expect(hash_in_array).not_to be_nil
    expect(hash_in_array([Bitcoin])).to eq([1,4])
  end
end